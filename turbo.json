{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "env": ["NODE_ENV"],
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "serve": {
      "dependsOn": ["build"],
      "cache": false
    },
    "lint": {
      "outputs": [],
      "inputs": [
        "../../configs/prettier/*",
        "../../configs/eslint/*",
        "../../.eslintrc.js",
        ".eslintrc.js",
        "src/**/*.{js,jsx,ts,tsx}"
      ]
    },
    "tscheck": {
      "outputs": [".types/**"],
      "inputs": ["../../configs/ts/*.json", "tsconfig.json", "src/**/*.{js,jsx,ts,tsx,json}"]
    },
    "bookshelf#tscheck": {
      "dependsOn": ["book#tscheck", "booklist#tscheck"],
      "outputs": [".types/**"],
      "inputs": ["../../configs/ts/*.json", "tsconfig.json", "src/**/*.{js,jsx,ts,tsx,json}"]
    },
    "booklist#tscheck": {
      "dependsOn": ["book#tscheck"],
      "outputs": [".types/**"],
      "inputs": ["../../configs/ts/*.json", "tsconfig.json", "src/**/*.{js,jsx,ts,tsx,json}"]
    },
    "dev": {
      "cache": false
    }
  }
}
