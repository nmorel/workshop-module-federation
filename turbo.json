{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "$NODE_ENV", "$FAST_REFRESH"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "tscheck": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
